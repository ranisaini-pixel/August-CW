export declare const DEFAULT_PROFANITY: string[];
export interface ProfanityFilterOptions {
    matchSubstrings?: boolean;
    wordBoundary?: string;
}
export declare function buildProfanityFilter(blocklist: string[], options?: ProfanityFilterOptions): (word: string) => boolean;
